{
  "version": 2,
  "name": "todoreact",
  "builds": [
    {
      "src": "todoreact/package.json",
      "use": "@now/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "todoreact/$1" }]
}
